= link_to t(:create_new_stream), new_stream_path

%table
  %tr
    %td= t(:title)
    %td= t(:url)
    
  - @streams.each do |stream|
    %tr
      %td= link_to stream.title, stream_path(stream)
      %td= stream.url