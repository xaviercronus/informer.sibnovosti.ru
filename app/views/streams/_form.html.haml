- if @stream.errors.any?
  %p.error= @stream.errors.full_messages.first

= form_for @stream do |f|
  = f.label :title
  = f.text_field :title
  %br
  = f.label :url
  = f.text_field :url
  %br
  = f.submit "Create new stream", :disable_with => 'Submiting...'